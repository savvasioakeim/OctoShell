{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main OctoShell window. Keep allow-destroy: Tauri's onCloseRequested wrapper closes a window by calling destroy(), so the moment anything registers that listener here, a missing allow-destroy makes the window silently refuse to close. It cost hours to find once already.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "notification:default",
    "sql:default",
    "sql:allow-execute",
    "sql:allow-select",
    "core:window:allow-minimize",
    "core:window:allow-maximize",
    "core:window:allow-unmaximize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "core:window:allow-start-dragging",
    "core:window:allow-is-maximized",
    "core:window:allow-set-focus",
    "core:webview:allow-create-webview-window"
  ]
}
